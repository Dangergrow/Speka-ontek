// Excel - сохранение и загрузка
function save(){if(!T.length)return toast('Нет таблиц!',0);const wb=new ExcelJS.Workbook();const ws=wb.addWorksheet('Заказы');let cr=1;const mc=Math.max(...T.map(t=>t.cols.length),5);ws.columns=[{width:6},...Array(mc).fill({width:24})];T.forEach(td=>{const ni=ci(td.cols,'Наименование');if(ni>=0)ws.getColumn(ni+2).width=88;const qi=getQi(td.cols);if(qi>=0)ws.getColumn(qi+2).width=18;const pi=ci(td.cols,'Цена');if(pi>=0)ws.getColumn(pi+2).width=22;const toti=ci(td.cols,'Стоимость');if(toti>=0)ws.getColumn(toti+2).width=22;});for(let ti=0;ti<T.length;ti++){const td=T[ti],tc=td.cols.length,qi=getQi(td.cols),pi=ci(td.cols,'Цена'),toti=ci(td.cols,'Стоимость'),ni=ci(td.cols,'Наименование');const hr=ws.getRow(cr);hr.getCell(1).value='№';hr.getCell(1).font={bold:true,size:12,color:{argb:'FFCBD5E1'},name:'Calibri'};hr.getCell(1).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF64748B'}};hr.getCell(1).alignment={horizontal:'center',vertical:'middle'};hdrs(td.cols,td.currency).forEach((h,ci)=>{const c=hr.getCell(ci+2);c.value=h;c.font={bold:true,size:12,color:{argb:'FFFFFFFF'},name:'Calibri'};c.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF64748B'}};c.alignment={horizontal:'center',vertical:'middle',wrapText:true};c.border={top:{style:'thin',color:{argb:'FF475569'}},bottom:{style:'thin',color:{argb:'FF475569'}},left:{style:'thin',color:{argb:'FF475569'}},right:{style:'thin',color:{argb:'FF475569'}}};});hr.height=28;cr++;const fdr=cr;for(let r=0;r<td.rows.length;r++){const row=ws.getRow(cr),bg=r%2===0?'FFEFF6FF':'FFDBEAFE';row.getCell(1).value=r+1;row.getCell(1).font={size:9,color:{argb:'FF94A3B8'},name:'Calibri'};row.getCell(1).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFF1F5F9'}};row.getCell(1).alignment={horizontal:'center',vertical:'middle'};for(let c=0;c<tc;c++){const cell=row.getCell(c+2);if(c===toti)cell.value={formula:`${cA(cr,qi+2)}*${cA(cr,pi+2)}`,result:pn(td.rows[r][c])||0};else if(c===qi||c===pi){const v=pn(td.rows[r][c]);cell.value=isNaN(v)?td.rows[r][c]:v;}else cell.value=td.rows[r][c]??'';if(c===toti||c===qi||c===pi)cell.numFmt='#,##0.00';cell.font={size:11,color:{argb:'FF1E293B'},name:'Calibri'};cell.fill={type:'pattern',pattern:'solid',fgColor:{argb:bg}};cell.alignment={horizontal:'center',vertical:'middle',wrapText:true};cell.border={top:{style:'thin',color:{argb:'FFBFDBFE'}},bottom:{style:'thin',color:{argb:'FFBFDBFE'}},left:{style:'thin',color:{argb:'FFBFDBFE'}},right:{style:'thin',color:{argb:'FFBFDBFE'}}};if(c===ni)cell.alignment.horizontal='left';if(c===qi||c===pi||c===toti)cell.alignment.horizontal='right';}cr++;}const ldr=cr-1,totr=ws.getRow(cr);totr.getCell(1).value='';totr.getCell(1).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFE2E8F0'}};for(let c=0;c<tc;c++){const cell=totr.getCell(c+2);if(c===(toti>=0?toti-1:tc-2))cell.value='Итого';else if(c===toti)cell.value={formula:`SUM(${cA(fdr,toti+2)}:${cA(ldr,toti+2)})`,result:sumT(td)};if(c===toti)cell.numFmt='#,##0.00';cell.font={bold:true,size:12,name:'Calibri'};cell.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFE2E8F0'}};cell.alignment={horizontal:'right',vertical:'middle',wrapText:true};cell.border={top:{style:'medium',color:{argb:'FF64748B'}},bottom:{style:'medium',color:{argb:'FF64748B'}},left:{style:'thin',color:{argb:'FFBFDBFE'}},right:{style:'thin',color:{argb:'FFBFDBFE'}}};}totr.height=28;cr++;if(ti<T.length-1)cr+=2;}wb.xlsx.writeBuffer().then(buf=>{const fn=`Заказы_ONTEK_${new Date().toISOString().slice(0,10)}.xlsx`;if(window.pywebview&&window.pywebview.api){const b64=btoa(String.fromCharCode(...new Uint8Array(buf)));window.pywebview.api.save_file(b64,fn).then(r=>{const j=JSON.parse(r);if(j.success)toast('✅ Сохранено!');else toast('Отменено',false);});}else{const blob=new Blob([buf],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=fn;document.body.appendChild(a);a.click();document.body.removeChild(a);URL.revokeObjectURL(url);toast('✅ Сохранено!');}}).catch(e=>{toast('Ошибка сохранения',false);});}

function load(file){const r=new FileReader();r.onload=e=>{try{const wb=XLSX.read(new Uint8Array(e.target.result),{type:'array'});const a=getArea();a.innerHTML='';T.length=0;idC=0;actId=null;hist.length=0;wb.SheetNames.forEach(n=>{const raw=XLSX.utils.sheet_to_json(wb.Sheets[n],{header:1,defval:''});if(!raw.length)return;let cur=null,ec=0;for(let i=0;i<raw.length;i++){const row=raw[i],emp=!row||row.every(c=>String(c??'').trim()==='');if(emp){ec++;if(ec>=2&&cur&&cur.rows.length){fin(cur);cur=null;}continue;}ec=0;const f=String(row[0]||'').trim();if(f.toLowerCase()==='итого'){if(cur&&cur.rows.length){fin(cur);cur=null;}continue;}if(row.some(c=>['артикул','наименование','ко-во','количество','цена','стоимость'].some(k=>String(c||'').toLowerCase().includes(k)))){if(cur&&cur.rows.length)fin(cur);const hd=row.map(c=>String(c||'').trim().replace(/,?\s*(USD|RUB)\s*с НДС/i,''));let det=row.join(' ').includes('USD')?'USD':'RUB';cur={cols:hd.filter(h=>h),rows:[],currency:det};if(!cur.cols.length)cur.cols=[...DC];continue;}if(!cur)cur={cols:[...DC],rows:[],currency:'RUB'};const dr=row.map(c=>String(c??'').trim());while(dr.length<cur.cols.length)dr.push('');cur.rows.push(dr.slice(0,cur.cols.length));}if(cur&&cur.rows.length)fin(cur);function fin(c){if(!c.cols.length)c.cols=[...DC];if(!c.rows.length)c.rows=[Array(c.cols.length).fill('')];idC++;T.push({id:idC,cols:c.cols,rows:c.rows,currency:c.currency,el:null,card:null});}});T.forEach(td=>{const card=buildCardDOM(td);a.appendChild(card);td.card=card;render(td);});upEmpty();if(!T.length)addTable('USD');else{setAct(T[0].id);toast(`Загружено: ${T.length} табл.`);}}catch(er){toast('Ошибка',0);if(!T.length)addTable('USD');}};r.readAsArrayBuffer(file);}

async function loadViaDialog(){if(window.pywebview&&window.pywebview.api){const r=JSON.parse(await window.pywebview.api.load_file());if(r.success){const bs=atob(r.data);const bytes=new Uint8Array(bs.length);for(let i=0;i<bs.length;i++)bytes[i]=bs.charCodeAt(i);const wb=XLSX.read(bytes,{type:'array'});const a=getArea();a.innerHTML='';T.length=0;idC=0;actId=null;hist.length=0;wb.SheetNames.forEach(n=>{const raw=XLSX.utils.sheet_to_json(wb.Sheets[n],{header:1,defval:''});if(!raw.length)return;let cur=null,ec=0;for(let i=0;i<raw.length;i++){const row=raw[i],emp=!row||row.every(c=>String(c??'').trim()==='');if(emp){ec++;if(ec>=2&&cur&&cur.rows.length){fin(cur);cur=null;}continue;}ec=0;const f=String(row[0]||'').trim();if(f.toLowerCase()==='итого'){if(cur&&cur.rows.length){fin(cur);cur=null;}continue;}if(row.some(c=>['артикул','наименование','ко-во','количество','цена','стоимость'].some(k=>String(c||'').toLowerCase().includes(k)))){if(cur&&cur.rows.length)fin(cur);const hd=row.map(c=>String(c||'').trim().replace(/,?\s*(USD|RUB)\s*с НДС/i,''));let det=row.join(' ').includes('USD')?'USD':'RUB';cur={cols:hd.filter(h=>h),rows:[],currency:det};if(!cur.cols.length)cur.cols=[...DC];continue;}if(!cur)cur={cols:[...DC],rows:[],currency:'RUB'};const dr=row.map(c=>String(c??'').trim());while(dr.length<cur.cols.length)dr.push('');cur.rows.push(dr.slice(0,cur.cols.length));}if(cur&&cur.rows.length)fin(cur);function fin(c){if(!c.cols.length)c.cols=[...DC];if(!c.rows.length)c.rows=[Array(c.cols.length).fill('')];idC++;T.push({id:idC,cols:c.cols,rows:c.rows,currency:c.currency,el:null,card:null});}});T.forEach(td=>{const card=buildCardDOM(td);a.appendChild(card);td.card=card;render(td);});upEmpty();if(!T.length)addTable('USD');else{setAct(T[0].id);toast(`Загружено: ${T.length} табл.`);}}else Q('#fileInput').click();}